My Javascript tutorial
let
Let is said to confine the scope of the variable to within the block (curly braces {} ) it was declared in.  If the variable is in the arguement (parameter) list of a funtion 
then it is unecessary (if fact not possible) to declare it within the block of the function definition.

